# Single-stage build (anti-pattern: retains compilers and devDependencies)
FROM node:20-alpine

WORKDIR /usr/src/app

COPY package*.json tsconfig.json ./
RUN npm install

COPY src/ ./src/
RUN npx tsc

EXPOSE 3000

CMD ["node", "dist/server.js"]
