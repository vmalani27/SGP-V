node_modules/
.next/
*.local
*.tsbuildinfo
next-app/.content

# Publish artifacts (regenerated by scripts/generate_manifest.py)
out/

# Vagrant build / runtime state (VM disks, private keys, metadata)
.vagrant/

# Secrets — global catch-all
.env
.env.*
!.env.sample
!.env.*.sample
credentials.json
service-account*.json
*.pem
*.key
*accessKeys*
*.csv

# Per-environment sealed config + firebase service-account keys
/environments/*/firebase/
/environments/**/FIREBASE_CREDS_*.json
/environments/


# Build artifacts & compiled binaries
*.zip
worker/*.zip
cli/labops.exe
cli/labops-darwin-amd64
cli/labops-linux-amd64
*.tar
!orchestrator/lab-images/preloads/*.tar
*.tar.gz


